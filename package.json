{
  "name": "openmsp-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel run dev",
    "build": "pnpm --recursive run build",
    "build:agent": "cd apps/agent && go build -o bin/openmsp-agent cmd/agent/main.go",
    "lint": "pnpm --recursive run lint",
    "typecheck": "pnpm --recursive run typecheck",
    "test": "pnpm --recursive run test"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
